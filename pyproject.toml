[project]
name = "property-number-extractor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pypdf2>=3.0.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.15.0",
]
